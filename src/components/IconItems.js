const IconItems = ({ children }) => {
  return (
    <div
      className="d-flex justify-content-md-start justify-content-center"
      style={{ marginTop: "30px" }}
    >
      {children}
    </div>
  );
};

export default IconItems;
